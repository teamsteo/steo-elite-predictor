name: Live Calibration Scan

# Scan automatique des matchs football live dans la fenêtre de pari mi-temps.
# Déclenche le pipeline de recalibration sur Vercel, qui :
#   1. Récupère les matchs ESPN live
#   2. Filtre la FENÊTRE DE PARI [42′, 55′] (mi-temps + début 2e MT)
#   3. Pour chaque match dans la fenêtre : fetch Understat xG + pipeline bayésien
#   4. Publie les value bets (confidence >= 50) sur Telegram DM
#   5. 🔒 ANTI-DOUBLON : jamais 2 publications pour le même match
#
# Stratégie cron : runs toutes les 10 min pendant les plages de matchs.
# Pourquoi si fréquent ? GitHub Actions peut avoir 5-15 min de retard.
# Avec l'anti-doublon côté serveur, les runs redondants sont des no-op
# quasi-gratuits (~3s de runtime), mais garantissent qu'ON NE RATE JAMAIS
# la fenêtre de pari d'un match.
#
# Plages couvertes (UTC) :
#   - Ven/Sam/Dim 12:00-20:50 (weekend : PL, Liga, Serie A, Bundesliga, L1)
#   - Mar/Mer/Jeu 17:00-20:50 (Ligue des Champions, Europa League)
#   - Lun exclu (rares matchs) — à ajouter manuellement via workflow_dispatch

on:
  schedule:
    # Weekend : toutes les 10 min de 12:00 à 20:50 UTC
    - cron: '*/10 12-20 * * 5,6,0'
    # Semaine (UCL/UEL mardi-mercredi-jeudi) : toutes les 10 min de 17:00 à 20:50
    - cron: '*/10 17-20 * * 2,3,4'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (scan sans publication Telegram)'
        required: false
        type: boolean
        default: false

permissions:
  contents: read

concurrency:
  group: live-calibration-scan
  cancel-in-progress: false

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      # Les secrets doivent être exposés via env pour être lisibles dans run:
      LIVE_CALIB_SECRET: ${{ secrets.LIVE_CALIB_SECRET }}
      CRON_SECRET: ${{ secrets.CRON_SECRET }}

    steps:
      - name: Trigger Vercel scan endpoint
        id: scan
        run: |
          set -euo pipefail
          echo "🔄 [$(date -u +%Y-%m-%dT%H:%M:%SZ)] Starting live calibration scan..."

          ENDPOINT="${{ vars.LIVE_CALIBRATION_URL || 'https://my-project-zeta-five-85.vercel.app/api/live-calibration/scan' }}"
          echo "Endpoint: $ENDPOINT"

          # Si dry_run, on appelle /backtest?publish=false au lieu de /scan
          if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
            ENDPOINT="${ENDPOINT%/scan}/backtest?publish=false"
            echo "🧪 Dry-run mode → $ENDPOINT"
          fi

          # Token d'auth : LIVE_CALIB_SECRET en priorité, fallback CRON_SECRET
          AUTH_TOKEN="${LIVE_CALIB_SECRET:-${CRON_SECRET:-}}"
          if [ -z "$AUTH_TOKEN" ]; then
            echo "❌ Aucun token d'auth disponible (LIVE_CALIB_SECRET ou CRON_SECRET)"
            exit 1
          fi
          echo "🔑 Auth token chargé (${#AUTH_TOKEN} chars)"

          RESPONSE=$(curl -sS -X POST "$ENDPOINT" \
            -H "Authorization: Bearer $AUTH_TOKEN" \
            -H "Content-Type: application/json" \
            -H "User-Agent: GitHubActions-LiveCalibration/1.0" \
            -w "\n---HTTP_STATUS:%{http_code}---" \
            --max-time 240 \
            -d '{}' || echo "CURL_ERROR")

          # Extract status code
          # `|| true` : sous set -euo pipefail, un grep sans match (curl error
          # → RESPONSE=CURL_ERROR) tuerait le script ICI, avant le traitement
          # d'erreur propre plus bas (HTTP_STATUS vide → exit 1 propre).
          HTTP_STATUS=$(echo "$RESPONSE" | grep -oE 'HTTP_STATUS:[0-9]+' | cut -d: -f2 || true)
          BODY=$(echo "$RESPONSE" | sed 's/---HTTP_STATUS:[0-9]*---//')

          echo "HTTP status: $HTTP_STATUS"
          echo "Response body:"
          echo "$BODY" | python3 -m json.tool 2>/dev/null || echo "$BODY"

          # Set outputs pour le step suivant
          echo "http_status=$HTTP_STATUS" >> $GITHUB_OUTPUT
          echo "response<<EOF" >> $GITHUB_OUTPUT
          echo "$BODY" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

          # Fail si HTTP 5xx ou curl error
          if [[ "$HTTP_STATUS" =~ ^5 ]] || [[ "$HTTP_STATUS" == "" ]]; then
            echo "❌ Scan failed with HTTP $HTTP_STATUS"
            exit 1
          fi

          echo "✅ Scan completed (HTTP $HTTP_STATUS)"

      - name: Summary
        if: always()
        run: |
          echo "## 🎯 Live Calibration Scan Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Timestamp**: $(date -u +%Y-%m-%dT%H:%M:%SZ)" >> $GITHUB_STEP_SUMMARY
          echo "- **HTTP status**: ${{ steps.scan.outputs.http_status }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Response" >> $GITHUB_STEP_SUMMARY
          echo '```json' >> $GITHUB_STEP_SUMMARY
          echo '${{ steps.scan.outputs.response }}' >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

      - name: Alert on failure
        if: failure()
        run: |
          echo "❌ Live calibration scan failed at $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          echo "Check the logs above for details."
          # Future: ajouter notification Discord/Slack ici si besoin
          exit 0
