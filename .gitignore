# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
.z-ai-config
dev.log
test
prompt

server.log
# Skills directory
/skills/
# Tool results (temporary)
tool-results/
# ML virtual environment
ml/venv/

# Backtest sandbox (copies dérivées de src/lib/liveCalibration, régénérables)
scripts/backtest_env/lib_fixed/
scripts/backtest_env/lib_buggy/
scripts/backtest_env/*.log

__pycache__/

# Backups locaux (archives + dumps DB)
backups/
# Credentials miroirs GitLab/Bitbucket (jamais versionnés)
scripts/.backup_env

# Log du miroir GitLab (hook pre-push)
scripts/gitlab-mirror.log

# ═══ SÉCURITÉ ANTI-PARASITES (Task 20) ═══
# Scripts avec credentials en dur — JAMAIS versionner (cause des commits
# parasites UUID 224c705/7e3b084/41be846 refusés par GitHub Push Protection)
scripts/add_gh_secret.py
scripts/push_fix.py
# Binaires lourds à la racine (repo*.tar.gz 26 Mo, .pkl junk des commits parasites)
/repo*.tar.gz
/*.pkl
# Caches xlsx binaires régénérables
scripts/.xlsx_cache/
# Fichiers temporaires à suffixe aléatoire (artefacts crash, ex: .skysports_tennis.json.G7Ogkx)
.*.json.??????
